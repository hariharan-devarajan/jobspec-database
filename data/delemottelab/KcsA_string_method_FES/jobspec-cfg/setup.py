from setuptools import find_packages, setup

setup(
    name='src',
    # packages=find_packages(where='src'),
    packages=find_packages(),
    version='0.1.0',
    description='String method simulations of KcsA with and without lipids bound',
    author='sperezconesa',
    license='MIT',
)
